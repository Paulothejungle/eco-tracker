<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eco-Tracker</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-100 min-h-screen p-4 md:p-10">
    <div class="max-w-xl mx-auto bg-white shadow-lg rounded-2xl p-6">
        <div class="flex items-center gap-2 mb-6">
            <span class="text-3xl">🍃</span>
            <h1 class="text-2xl font-bold text-slate-800">Eco-Tracker Listrik</h1>
        </div>

        <form action="/add" method="POST" class="space-y-4 mb-10 bg-slate-50 p-4 rounded-xl">
            <div>
                <label class="block text-sm font-medium text-slate-600">Tanggal</label>
                <input type="date" name="date" required class="w-full mt-1 border-slate-200 rounded-lg p-2 focus:ring-2 focus:ring-green-500 outline-none">
            </div>
            <div>
                <label class="block text-sm font-medium text-slate-600">Penggunaan (kWh)</label>
                <input type="number" step="0.01" name="kwh" placeholder="Contoh: 12.5" required class="w-full mt-1 border-slate-200 rounded-lg p-2 focus:ring-2 focus:ring-green-500 outline-none">
            </div>
            <button type="submit" class="w-full bg-green-600 text-white font-semibold py-2 rounded-lg hover:bg-green-700 transition">Simpan Penggunaan</button>
        </form>

        <h2 class="text-lg font-bold text-slate-700 mb-4">Riwayat Penggunaan</h2>
        <div class="space-y-3">
            <% if (history.length === 0) { %>
                <p class="text-slate-400 text-center italic">Belum ada data tersedia.</p>
            <% } %>
            <% history.forEach(item => { %>
                <div class="flex justify-between items-center p-3 border-b border-slate-100">
                    <span class="text-slate-600"><%= item.date %></span>
                    <span class="font-mono font-bold text-green-700"><%= item.kwh %> kWh</span>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>